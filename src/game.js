window.addEventListener("load", (event) => {
  const canvas = document.getElementById("root");
  const ctx = canvas.getContext("2d");

  const fps = 25;

  let frame = 0;

  setInterval(() => {}, 25000 / fps);
});
